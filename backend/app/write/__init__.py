"""Write-path modules (plan todos 14/15): preview checks, form-replay payload
builders, canonicalization, confirm-token store, and the CSRF surface.

Todo 15 (submission engine) consumes canonical.py + confirm.py UNCHANGED -
the canonical spec is a single shared source of truth by design.
"""
